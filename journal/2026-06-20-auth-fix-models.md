# 工作日志 — Auth 修复 / 模型重配 / 多模态

> 模型/端点名易变，按要求不写死细节；此处记通用结论。

## 预先任务清单
1. 修 auth 报错 + 按指定重配模型（删 qwen-max，加降级模型，配默认）
2. 多模态测试（图/PDF/代码）+ Cline 是否需多模态
3. 多轮记忆 / 技能失败 / cline+微信路由 测试 + 列已启用技能
4. 设置优化（带风险概率）+ 缓存决策 + 自动装有用技能
5. 所有改动测试成文档推 GitHub + 核验上一大 prompt 完成度

## 完成报告
| # | 任务 | 结果 |
|---|------|------|
| 1 | auth+模型 | ✅ 诊断出 auth 根因=指定的 3.7 系列模型在本账户/端点 **403 无权限**；唯一可用=降级模型。已设其为主模型、删 qwen-max、注册其余 4 个、Cline 同步、config valid、网关恢复 |
| 2 | 多模态 | ✅ 模型接受图片输入；PDF/Word/代码由 OpenClaw 文件工具转文本。**Cline 不需多模态**（OpenClaw 转文字喂它） |
| 3 | 技能/路由/记忆 | 见报告（45/83 技能就绪；cline/wechat/lobster 触发式联想已 Ready+可见；记忆=每会话持久，pruning 只裁工具输出不丢对话） |
| 4 | 设置/缓存 | cacheRetention=long 已设；compat 字段会破坏校验故未加→此端点缓存不主动路由（诚实）；详见报告概率说明 |
| 5 | 文档/推送/核验 | 本日志 + auto-push；上一大 prompt 核验见报告 |

## 关键结论（含后续 live 实测修正）
- **403 是限流误判，不是无权限**：带 2s 间隔重测，`0520/0517/preview/01-23` **全部 [OK]**。已把主模型设为你要的 **qwen3.7-max-2026-05-20**；Cline 同步。
- **🐛 找到并修复"技能调用失败"真 bug**：此 MaaS 端点不接受 `role="tool"`（只认老式 `function`），导致 agent 用工具时 `400 InvalidParameter`。**把 provider `api` 从 `openai-responses` 改为 `openai-completions` 后修复**，工具调用实测返回 `HELLO_OK`。
- **plugins.allow** 已设为 8 个可信插件白名单（消除告警 + 收敛非可信插件自动加载）。

## live 实测全过（不再是分析推断）
| 测试 | 结果 |
|------|------|
| 多轮记忆（同会话两轮） | ✅ 第2轮答出"42"，记得住 |
| 多模态（图片输入） | ✅ 模型处理了图片 |
| 工具/技能调用 | ✅ 修 api 后 `exec` 实测成功 |
| 微信路由（提微信→WeFlow） | ✅ agent 自己查到 zoo 群 |
| 模型可用性 | ✅ 4 个全 OK |

- **adaptive 思考 + 30m pruning 不变蠢**：pruning 只裁旧工具输出，对话文本始终保留。
