# 2026-06-21 — 登记并启用 qwen3.6-max-preview 默认模型

## 1. 任务背景与目标
- 用户请求登记 `qwen3.6-max-preview` 模型并将其设为默认模型。
- 需要以脚本化或自动化方式执行，确保 OpenClaw 网关配置、Cline 代理配置和全局配置同步更新，且不会由于 PowerShell json 转换损坏 openclaw.json 的 models 数组。

## 2. 自动化工具与执行过程
为保证配置的安全和正确，我们编写了 Python 脚本 `tools/register_model.py` 及 PowerShell 封装脚本 `tools/apply-qwen36.ps1`：
- **安全备份**：在修改前，自动将 `openclaw.json`、`config.yml` 和 Cline 的 `providers.json` 备份到同一目录下的带时间戳 `.bak.<timestamp>` 文件中。
- **网关生命周期管理**：在修改配置前，安全地停止 OpenClaw 守护进程和网关，并在配置修改完毕后重新拉起并验证监听状态。
- **精确 JSON 处理**：使用 Python 内置 JSON 模块处理，完全避开了 PowerShell `ConvertTo-Json` 的深度截断和格式损坏风险，严格保留 2 空格缩进。

## 3. 配置更新细节
- **OpenClaw 主配置 (`C:\Users\10979\.openclaw\openclaw.json`)**：
  - 在 `models.providers.openai.models` 列表中安全登记了 `qwen3.6-max-preview`（继承 `qwen3.7-max-preview` 的推理与超参设置：reasoning=true, contextWindow=131072, contextTokens=80000）。
  - 将 `agents.defaults.model.primary` 设为 `openai/qwen3.6-max-preview` 作为默认模型。
  - 在 `agents.defaults.models` 中添加 `"openai/qwen3.6-max-preview": {}` 注册覆盖项。
- **Cline 配置 (`C:\Users\10979\.cline\data\settings\providers.json`)**：
  - 更新 `providers.openai-compatible.settings.model` 值为 `qwen3.6-max-preview`。
- **全局配置 (`C:\Users\10979\.openclaw\config.yml`)**：
  - 正则匹配并安全替换 `model:` 行为 `model: "qwen3.6-max-preview"`。

## 4. 验证结果
- 网关已成功重启并监听到 `18789` 端口。
- 配置文件格式均正确，且更新项已全部生效。
