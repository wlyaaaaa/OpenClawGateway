# 工作日志 — 2026-06-20

> 工作流：执行前给**任务清单**，执行后给**报告 + 完成清单**，归档于此供查阅。
> 仓库由 `OpenClawGateway AutoPush` 计划任务定时自动推送到 GitHub。

## 本轮预先任务清单
1. 深读官方 github/docs 找优化点（前提：保住 qwen3.7-max 最高思考、AI 不变蠢）
2. session pruning 调温和（5m→30m，不特别丢上下文）+ 保住最高思考
3. WeFlow 开源（翻公开仓库）
4. WeFlow 开机看门狗 + 微信开机自启
5. WeFlow 检查调优
6. 归档机制 + 定时自动推 GitHub

## 完成报告
| # | 任务 | 结果 |
|---|------|------|
| 1 | 官方文档优化 | 找到 **session-pruning**（已启用）、**prompt caching**（有风险，列为付费验证项）、Lobster（未装）；doctor 提示**勿 `--fix`**（会加回 `"*"` 通配） |
| 2 | pruning 温和化 | `contextPruning.ttl 5m→30m`（只裁 >30min 旧工具输出，对话无损）；`thinkingDefault=adaptive`（难题自动拉满 + 可 `/think max`），最高思考可用 |
| 3 | WeFlow 开源 | 审计无密钥无隐私 → `gh repo edit --visibility public`，已 **PUBLIC** |
| 4 | WeFlow 看门狗 | `WeFlow Watchdog`（登录+15min 自愈）/`WeChat AutoStart`（登录自启）已注册 Ready；GUI 需会话，真·非登录见 `enable-autologin.ps1` |
| 5 | WeFlow 调优 | probe 全绿（会话/联系人/群成员104/朋友圈89）；心跳 [OK]；无需额外调优 |
| 6 | 归档+自动推送 | 本 journal + `tools\auto-archive-push.ps1` + `OpenClawGateway AutoPush` 计划任务 |

## 联动现状（已就位）
- `🦞 cline-coding` skill（✓Ready/可见）：提代码/多文件/调试 → 委托 Cline 省 token
- `💬 wechat` skill（✓Ready/可见）：提微信/群消息 → 调 WeFlow API
- 静态兜底在工作区 `TOOLS.md`

## 关键约束（遵守）
- 模型恒为 **qwen3.7-max**；思考 **adaptive**（最高可达/可手动 max）；pruning 仅裁旧工具输出，**不丢对话**。
- 机密永不入库；公开仓库推送前机密扫描守卫。
